spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: https://config-server-bank.azurewebsites.net
      #http://localhost:8762-> dev
      #http://config-server:8762-> docker
      #https://config-server-bank.azurewebsites.net -> pdr
